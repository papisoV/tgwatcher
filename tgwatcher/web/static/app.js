// app.js — Phase 3C complete
// All 77 functions + top-level state migrated to tgwatcher/web/static/app/*.js
// Loaded in dependency order via index.html (state → utils → api-client →
// sse → render → login → groups → messages → export → signal-export →
// dashboard → crawl → poll-listen-webhook → signal → ui).
// This file is intentionally empty; kept only as a marker for tooling that
// expects a top-level app.js.
